# Chain Garden Viewer

Decrypt and view the Chain Garden token. Enter the key to unlock.

## Deploy

```bash
npm install
npm run dev     # local
npm run build   # static export → out/
```

Deploy `out/` anywhere, or connect to Vercel for auto-deploy.
